{% extends 'base.html.twig' %}

{% block title 'Editer les taches' %}

{% block body %}
    <div class="container mt-4">
        <h1>Editer les taches</h1>
        {{ form_start(form) }}
            {{ form_rest(form) }}
            <button class="btn btn-primary">Editer</button>
            <a href="{{ path('property_index') }}" class="btn btn-secondary">Retour</a>
        {{ form_end(form) }}
        
    </div
{% endblock %}