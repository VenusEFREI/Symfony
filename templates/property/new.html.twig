{% extends 'base.html.twig' %}

{% block title 'Créer une tache' %}

{% block body %}
    <div class="container mt-4">
        <h1>Créer une taches</h1>
        {{ form_start(form) }}
            {{ form_rest(form) }}
            <button class="btn btn-primary">Créer</button>
            <a href="{{ path('property_index') }}" class="btn btn-secondary">Retour</a>
        {{ form_end(form) }}

    </div
    
{% endblock %}
